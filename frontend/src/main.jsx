import React,{useState}from'react';import{createRoot}from'react-dom/client';const API=import.meta.env.VITE_API_URL||'http://127.0.0.1:8000';function App(){const[t,setT]=useState(localStorage.token||'');const[f,setF]=useState({username:'',password:''});const[tab,setTab]=useState('login');const[m,setM]=useState('');async function go(){let r=await fetch(API+'/auth/'+tab,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)}),x=await r.json();if(!r.ok)return setM(x.detail||'Failed');if(tab==='login'){localStorage.token=x.access_token;setT(x.access_token)}else setM('Registered. Login now.')}if(!t)return <main><h1>Fraud Detection System</h1><p>ML-assisted transaction risk dashboard</p><button onClick={()=>setTab('login')}>Login</button><button onClick={()=>setTab('register')}>Register</button><input placeholder="Username" onChange={e=>setF({...f,username:e.target.value})}/><input type="password" placeholder="Password" onChange={e=>setF({...f,password:e.target.value})}/><button onClick={go}>{tab}</button><p>{m}</p></main>;return <main><h1>Fraud Detection Dashboard</h1><p>Authenticated session active.</p><p>Use the API dashboard or Swagger to score transactions with the Isolation Forest demo model.</p><a href={API+'/docs'} target="_blank">Open API Docs →</a><br/><button onClick={()=>{localStorage.clear();setT('')}}>Logout</button></main>}createRoot(document.getElementById('root')).render(<App/>);